---
layout: main.njk
---

<main class="section_places">
  {# ===================== CONTENU MARKDOWN ===================== #}
  {{ content | safe }}

  {# ===================== DONNÉES JS ===================== #}
  {% if charts.place_types %}
  <script>
    window.place_types = {{ charts.place_types | dump | safe }};
  </script>
  {% endif %}
</main>
