---
layout: main.njk
---

<main class="section_garance">

  {# ===================== CONTENU MARKDOWN ===================== #}
  {{ content | safe }}

  {# ===================== DONNÉES JS ===================== #}
  {% if charts.agent_types %}
  <script>
    window.agent_types = {{ charts.agent_types | dump | safe }};
  </script>
  {% endif %}

  {% if charts.person_occupations %}
  <script>
    window.person_occupations = {{ charts.person_occupations | dump | safe }};
  </script>
  {% endif %}

  {% if charts.corporatebody_types %}
  <script>
    window.corporatebody_types = {{ charts.corporatebody_types | dump | safe }};
  </script>
  {% endif %}

  {% if charts.anf_types %}
  <script>
    window.anf_types = {{ charts.anf_types | dump | safe }};
  </script>
  {% endif %}

</main>
