---
layout: main.njk
---

<main class="section_places">

  {% if places_types_stats %}
  <section class="mb-5">
	<h3 class="text-center mt-4 mb-4">
		Cliquez sur les valeurs de la légende pour désactiver certains éléments.
	</h3>


    <h4 class="text-center mt-4 mb-4">
      Répartition des lieux par type
    </h4>

    <!-- Diagramme centré -->
    <div class="row justify-content-center">
      <div class="col-md-6 col-lg-5 text-center chart-wrapper">
        <canvas id="placesChart"></canvas>
	  </div>
    </div>

      {# ===================== SCRIPT ===================== #}
  {% if places_types_stats %}
<script>
  window.places_types_stats = {{ places_types_stats | dump | safe }};
</script>
{% endif %}

  </section>
  {% endif %}

  <!-- Contenu éditorial -->
  <section>
    {{ content | safe }}
  </section>

</main>
